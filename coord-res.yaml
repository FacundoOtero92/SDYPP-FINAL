spec:
  template:
    spec:
      containers:
      - name: coordinador
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            memory: "512Mi"
